<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List view for strat.module.link records -->
    <record id="view_strat_module_link_list" model="ir.ui.view">
        <field name="name">strat.module.link.list</field>
        <field name="model">strat.module.link</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="model_a"/>
                <field name="model_b"/>
                <field name="active"/>
            </list>
        </field>
    </record>

</odoo>
